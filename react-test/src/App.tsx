import React, { useState } from 'react';

import Button from './components/atoms/Button';

const App: React.FC = () => {
// Add code here to render the button and call the API on button click and then render then
// returned response from the API on the page.

// Call this API using a GET request
//
// https://jsonplaceholder.typicode.com/users
//
// Display the results on the page. Create some components to suit your needs.
// E.g. maybe a component to process the list of users.

}

export default App;
